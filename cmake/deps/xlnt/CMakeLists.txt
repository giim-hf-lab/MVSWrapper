FetchContent_Declare(XLNT
	GIT_REPOSITORY "$CACHE{__C_GITHUB_PREFIX}tfussell/xlnt.git"
	GIT_TAG "v$CACHE{__C_XLNT_VERSION}"
	GIT_SUBMODULES_RECURSE TRUE
	GIT_SHALLOW "${__G_FETCHCONTENT_GIT_SHALLOW}"
	GIT_REMOTE_UPDATE_STRATEGY CHECKOUT
	OVERRIDE_FIND_PACKAGE
)

find_package(XLNT "$CACHE{__C_XLNT_VERSION}" REQUIRED GLOBAL)
